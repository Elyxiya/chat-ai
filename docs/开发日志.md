# 开发日志

## 2026-05-26：Sprint 4 完成 — 全局搜索 + 文件上传 + 消息回复转发 + 群聊增强 + 用户主页 + 收藏置顶

### 概述

完成 Sprint 4 全部 6 个子任务（P0×2 + P1×2 + P2×2），涵盖全局消息搜索、文件上传系统完善、消息回复与转发、群聊增强、用户个人主页、消息收藏与会话置顶。

所有功能通过编译验证（后端 + 前端构建零错误），修复 4 项视觉/功能 Bug。

---

### 变更文件总览

| 文件 | 说明 |
|------|------|
| `apps/api/src/modules/chat/chat.controller.ts` | 新增搜索、置顶、转发、回复上下文、群公告、邀请链接端点 |
| `apps/api/src/modules/chat/chat.service.ts` | 搜索逻辑、置顶逻辑、群公告 CRUD、邀请链接生成/验证 |
| `apps/api/src/modules/upload/upload.controller.ts` | 文件上传/下载/信息查询端点 |
| `apps/api/src/modules/upload/upload.service.ts` | 文件上传业务逻辑 |
| `apps/api/src/modules/upload/minio.service.ts` | MinIO 对象存储封装 |
| `apps/api/src/modules/auth/auth.controller.ts` | 新增 changePassword 端点 |
| `apps/api/src/modules/user/user.controller.ts` | 用户资料/头像端点 |
| `apps/api/src/modules/user/user.service.ts` | 用户资料更新逻辑 |
| `apps/api/src/gateways/chat.gateway.ts` | 新增 reaction 事件广播 |
| `apps/api/prisma/schema.prisma` | 新增 Bookmark 模型，User/Message reverse relations |
| `apps/web/src/pages/ChatLayout.tsx` | 集成 GlobalSearchModal、MentionPicker |
| `apps/web/src/pages/PrivateChatPage.tsx` | 集成回复/转发/文件上传/表情反应逻辑 |
| `apps/web/src/pages/ProfilePage.tsx` | 用户个人主页（头像/昵称/bio/改密） |
| `apps/web/src/pages/SettingsPage.tsx` | 设置页面增强 |
| `apps/web/src/components/VirtualizedMessageList/VirtualizedMessageList.tsx` | 气泡高度修复，onReaction 透传 |
| `apps/web/src/components/MessageBubble/MessageBubble.tsx` | 回复/转发/收藏菜单项，气泡间距修复 |
| `apps/web/src/components/SessionList/SessionList.tsx` | 会话置顶，button 嵌套修复 |
| `apps/web/src/components/GlobalSearchModal/GlobalSearchModal.tsx` | **新增** 全局搜索模态框 |
| `apps/web/src/components/ForwardModal/ForwardModal.tsx` | **新增** 转发消息选择弹窗 |
| `apps/web/src/components/GroupDetailPanel/GroupDetailPanel.tsx` | **新增** 群聊详情面板 |
| `apps/web/src/components/MentionPicker/MentionPicker.tsx` | **新增** @提及选择器 |
| `apps/web/src/components/BookmarkPanel/BookmarkPanel.tsx` | **新增** 收藏面板 |
| `apps/web/src/components/FileUpload/FileUploadPanel.tsx` | **新增** 文件上传面板（拖拽/选择） |
| `apps/web/src/components/UserProfileCard/UserProfileCard.tsx` | **新增** 他人资料卡片 |
| `apps/web/src/stores/chat.store.ts` | 新增 addMessageReaction, socket reaction 监听 |
| `apps/web/src/stores/auth.store.ts` | 新增 setUser action |
| `apps/web/src/api/client.ts` | 新增 chatApi/walletApi 方法，changePassword |
| `apps/web/src/types/index.ts` | 新增 pinnedAt 字段 |

---

### P0: 全局消息搜索

**端点**:
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/chat/search` | 全局消息搜索（q, sessionId, types, dateFrom, dateTo, page, limit） |

**前端**:
- `GlobalSearchModal`: Ctrl+K 快捷键触发，搜索结果按会话分组，关键字高亮
- 点击搜索结果跳转到对应会话并定位到消息

**涉及文件**:
- `apps/api/src/modules/chat/chat.controller.ts` — `searchMessages()`
- `apps/api/src/modules/chat/chat.service.ts` — `searchMessages()`
- `apps/web/src/api/client.ts` — `chatApi.searchMessages()`
- `apps/web/src/components/GlobalSearchModal/GlobalSearchModal.tsx` — **新建**
- `apps/web/src/pages/ChatLayout.tsx` — 集成搜索弹窗

---

### P0: 文件上传系统完善

**端点**:
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/upload/files` | 上传文件 (multipart/form-data) |
| GET | `/api/v1/upload/files/:id` | 获取文件信息 |
| GET | `/api/v1/upload/files/:id/download` | 下载文件 |

**前端**:
- `FileUploadPanel`: 拖拽/选择上传区域，上传进度指示
- `MessageBubble` 图片消息渲染优化（点击放大）
- 文件类型消息渲染（文件名、大小、下载按钮）
- 支持多文件上传

**涉及文件**:
- `apps/api/src/modules/upload/upload.controller.ts`
- `apps/api/src/modules/upload/upload.service.ts`
- `apps/api/src/modules/upload/minio.service.ts`
- `apps/web/src/components/FileUpload/FileUploadPanel.tsx`
- `apps/web/src/components/MessageBubble/MessageBubble.tsx`

---

### P1: 消息回复与转发

**端点**:
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/chat/messages/:id/reply-context` | 获取被回复消息上下文 |
| POST | `/api/v1/chat/messages/forward` | 转发消息到目标会话 |

**前端**:
- 消息气泡菜单「回复」→ 输入区显示引用条（被回复者昵称 + 内容摘要）
- 消息气泡菜单「转发」→ `ForwardModal` 选择目标会话（多选）
- 被回复消息在气泡中以引用块显示（点击引用可滚动定位）
- 发送回复消息时通过 WebSocket 附加 `replyToId`

**涉及文件**:
- `apps/api/src/modules/chat/chat.controller.ts`
- `apps/api/src/modules/chat/chat.service.ts`
- `apps/web/src/components/MessageBubble/MessageBubble.tsx` — 回复/转发菜单项
- `apps/web/src/components/ForwardModal/ForwardModal.tsx` — **新建**
- `apps/web/src/pages/PrivateChatPage.tsx` — 回复状态管理 + 发送逻辑

---

### P1: 群聊增强

**端点**:
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/chat/sessions/:id/announcement` | 设置/更新群公告 |
| DELETE | `/api/v1/chat/sessions/:id/announcement` | 删除群公告 |
| POST | `/api/v1/chat/sessions/:id/invite-link` | 生成邀请链接 |
| POST | `/api/v1/chat/sessions/join-by-link` | 通过链接加入群聊 |
| GET | `/api/v1/chat/sessions/:id/members` | 群成员列表 |

**前端**:
- `GroupDetailPanel`: 群聊详情面板（公告显示/编辑入口，成员列表 + 角色标识）
- `MentionPicker`: @提及输入触发器 + 成员选择器

**涉及文件**:
- `apps/api/src/modules/chat/chat.controller.ts`
- `apps/api/src/modules/chat/chat.service.ts`
- `apps/web/src/components/GroupDetailPanel/GroupDetailPanel.tsx` — **新建**
- `apps/web/src/components/MentionPicker/MentionPicker.tsx` — **新建**
- `apps/web/src/pages/ChatLayout.tsx` — 集成 @提及

---

### P2: 用户个人主页

**端点**:
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/users/profile` | 获取当前用户资料 |
| PATCH | `/api/v1/users/profile` | 更新资料 |
| POST | `/api/v1/users/avatar` | 上传头像 |
| POST | `/api/v1/auth/change-password` | 修改密码 |
| GET | `/api/v1/users/profile/:id` | 查看他人资料 |

**前端**:
- `ProfilePage`: 头像编辑、昵称、bio、修改密码表单
- `UserProfileCard`: 查看他人资料的弹窗组件

**涉及文件**:
- `apps/api/src/modules/user/user.controller.ts`
- `apps/api/src/modules/user/user.service.ts`
- `apps/api/src/modules/auth/auth.controller.ts` — `changePassword()`
- `apps/web/src/pages/ProfilePage.tsx` — **新建**
- `apps/web/src/components/UserProfileCard/UserProfileCard.tsx` — **新建**

---

### P2: 消息收藏与会话置顶

**端点**:
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/chat/messages/:id/bookmark` | 收藏/取消收藏消息 |
| GET | `/api/v1/chat/bookmarks` | 获取收藏列表 |
| PATCH | `/api/v1/chat/sessions/:id/pin` | 置顶/取消置顶会话 |

**前端**:
- 消息菜单「收藏」按钮（Bookmark 状态切换图标）
- `BookmarkPanel`: 查看所有收藏消息的面板
- 会话列表置顶会话优先显示（`pinnedAt` 排序），已置顶会话分组在列表顶部

**涉及文件**:
- `apps/api/src/modules/chat/chat.controller.ts`
- `apps/api/src/modules/chat/chat.service.ts`
- `apps/web/src/stores/chat.store.ts`
- `apps/web/src/components/BookmarkPanel/BookmarkPanel.tsx` — **新建**
- `apps/web/src/components/SessionList/SessionList.tsx` — 置顶显示

---

### Bug 修复

#### 1. DOM 嵌套警告：`<button>` 嵌套 `<button>`

**根因**: SessionItem 使用 `<button>` 作为行容器，内部 Pin 按钮也是 `<button>`，违反 HTML 规范。

**修复**: 外层 `<button>` → `<div>` 配合 `role="button"`、`tabIndex={0}`、键盘事件处理。

#### 2. 表情回应功能不可用

**根因**: `VirtualizedMessageList` 未透传 `onReaction` prop 到 `MessageBubble`。

**修复**: 添加 `onReaction` 到 `VirtualizedMessageListProps` 接口和 Row 组件，在 `PrivateChatPage` 中实现 `handleReaction` 回调。

#### 3. 表情回应需要手动刷新才能看到

**根因**: 前端调用 API 后本地 store 未更新，且其他用户未收到 WebSocket 广播。

**修复**:
- 前端: 新增 `chat.store.ts` `addMessageReaction()` 方法，API 成功后立即更新本地 store
- 后端: 新增 `@SubscribeMessage('reaction')` 网关处理器，向会话房间广播
- 前端 socket listener: 监听 `reaction` 事件，无感知更新所有客户端

#### 4. 消息气泡重叠

**根因**: `getItemHeight()` 估算高度（单行文本 ~74px）低于实际渲染高度（~100px+），react-window 按估算分配空间导致重叠。

**修复**:
- `ROW_ESTIMATED_SIZE`: 88 → 120
- 各类型高度增大: text 60→110, image 340→360, file 90→130, audio 70→110, ai_response 100→140
- Row 组件添加 `px-5` 内边距
- 滚动阈值: SCROLL_BOTTOM_THRESHOLD 50 → 20
- 滚动机制: `setTimeout(50ms)` → `requestAnimationFrame`

---

### 构建验证

| 模块 | 命令 | 结果 |
|------|------|------|
| 后端 | `pnpm --filter ai-native-chat-api build` | ✅ 零错误 |
| 前端 | `pnpm --filter ai-native-chat-web build` | ✅ 零错误 |
| 类型检查 | `pnpm typecheck` | ✅ 通过 |

---

## 2026-05-25：Sprint 3 完成 — 移动端响应式 + Admin 后台 + 性能优化 + 深色模式

### 概述

完成项目开发计划中 Sprint 3 的全部 4 个子任务，覆盖移动端适配、后台管理、性能优化和深色模式打磨。

所有功能通过测试验证（前端 204 个 + 后端 484 个）。

---

### 3.1 移动端响应式适配

**变更文件**:
- [ChatLayout.tsx](apps/web/src/pages/ChatLayout.tsx) — 侧边栏抽屉式改造
- [MessageBubble.tsx](apps/web/src/components/MessageBubble/MessageBubble.tsx) — 移动端宽度优化

**要点**:
- 侧边栏在 `<md` 断点下折叠为 fixed 抽屉，`translate-x` 动画控制显隐
- 遮罩层（`bg-black/40`）点击关闭侧边栏
- 汉堡菜单按钮显示在 mobile header 中
- 消息气泡宽度：移动端 85%，桌面 70%

---

### 3.2 Admin 后台管理模块

**新增文件**:
- [admin.module.ts](apps/api/src/modules/admin/admin.module.ts)
- [admin.guard.ts](apps/api/src/modules/admin/admin.guard.ts) — JWT + DB 角色验证
- [admin.service.ts](apps/api/src/modules/admin/admin.service.ts) — 用户管理/设置/审计日志业务逻辑
- [admin.controller.ts](apps/api/src/modules/admin/admin.controller.ts) — REST 端点 + 仪表盘统计
- [AdminPage.tsx](apps/web/src/pages/AdminPage.tsx) — Users/Settings/Audit Logs 三标签页

**端点**:
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/admin/users` | 用户列表（分页+搜索+过滤） |
| PATCH | `/api/v1/admin/users/:id/status` | 禁言/解禁 |
| PATCH | `/api/v1/admin/users/:id/role` | 角色变更 |
| DELETE | `/api/v1/admin/users/:id` | 删除用户 |
| GET | `/api/v1/admin/audit-logs` | 审计日志 |
| GET | `/api/v1/admin/settings` | 系统设置 |
| PATCH | `/api/v1/admin/settings` | 更新设置 |
| GET | `/api/v1/admin/stats` | 仪表盘统计 |

**Prisma 变更**: User 模型新增 `role` 字段（默认 `"user"`）

---

### 3.3 性能优化

**虚拟滚动**:
- [VirtualizedMessageList.tsx](apps/web/src/components/VirtualizedMessageList/VirtualizedMessageList.tsx) — 基于 react-window VariableSizeList
- 仅渲染可视区域消息，`overscanCount=5` 保证滚动流畅
- 自动滚动到底部（检测用户位置，不在底部时不抢滚动）
- "↓ New messages" 浮动按钮
- 通过 ResizeObserver 自适应容器大小

**图片懒加载**:
- [LazyImage.tsx](apps/web/src/components/LazyImage/LazyImage.tsx) — IntersectionObserver 实现
- 200px 预加载边界，骨架屏占位
- 应用到 MessageBubble 的图片消息和用户头像

---

### 3.4 深色模式打磨

**对比度修复**:
- `--color-text-secondary`: `#94a3b8` → `#cbd5e1`（WCAG AA 合规）
- `--color-border`: `#334155` → `#475569`（更清晰的边界）
- 滚动条颜色: `#475569` → `#64748b`
- Tailwind config: `text-primary` → `text` 映射修正

**主题过渡动画**:
- 新增 `theme-ready` 类控制 CSS transitions
- 通过 `requestAnimationFrame` 延迟启用，避免首次加载闪烁

**无闪烁主题切换**:
- index.html 添加内联 script，优先读取 localStorage 设置 `dark` 类
- 在 React 初始化前完成主题应用

---

## 2026-05-24：修复生产环境遗留问题（第二轮）

### 概述

在第一轮修复基础上，进一步解决了 DeepSeek Embedding 端点在 `api.deepseek.com` 上不存在的问题、Redis LRANGE 参数兼容性问题，并新增了 EmbeddingService 抽象层。

所有修复均通过测试验证（后端 418 个 + 前端 117 个）。

---

### 1. Embedding 服务抽象（替换 DeepSeek Embedding）

**根因**: `api.deepseek.com` 不提供 embedding API 端点（`/v1/embeddings` 返回 404），需要更换 embedding 提供商。

**修复**:
- 创建 [embedding.service.ts](../apps/api/src/modules/llm/providers/embedding.service.ts) — 可注入的 EmbeddingService，支持多提供商：
  - `openai`: 使用 `text-embedding-3-small`（1536 维，兼容当前数据库 schema）
  - `deepseek`: 作为兼容选项保留（会优雅地返回空数组）
- 通过 `.env` 的 `EMBEDDING_PROVIDER=openai|deepseek` 切换
- RAG 引擎 (`rag-engine.service.ts`) 注入 EmbeddingService，不再直接依赖 DeepSeekProvider
- ToolRegistry 也注入 EmbeddingService 并传递给 RagEngine

**环境变量新增**:
```
EMBEDDING_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_EMBEDDING_URL=https://api.openai.com/v1/embeddings
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
```

**变更文件**:
- [.env](../.env)
- [apps/api/src/modules/llm/providers/embedding.service.ts](../apps/api/src/modules/llm/providers/embedding.service.ts) — **新建**
- [apps/api/src/modules/llm/llm.module.ts](../apps/api/src/modules/llm/llm.module.ts)
- [apps/api/src/modules/agent/rag/rag-engine.service.ts](../apps/api/src/modules/agent/rag/rag-engine.service.ts)
- [apps/api/src/modules/agent/tools/tool-registry.service.ts](../apps/api/src/modules/agent/tools/tool-registry.service.ts)

---

### 2. Redis LRANGE 参数兼容性修复

**根因**: ioredis v5 与 Redis 服务器之间可能出现 `LRANGE` 命令参数序列化兼容性问题，报 `ERR value is not an integer or out of range`。MemoryService 虽然已有 try-catch 保护，但日志噪音较大。

**修复**:
- `RedisService.lrange` 对 `start`/`stop` 参数使用 `Math.floor()` 确保整数，同时改为捕获异常返回 `[]`，不再向上抛
- `RedisService.ltrim`、`RedisService.expire`、`RedisService.set` 同样使用 `Math.floor()` 防御
- MemoryService 中 `getShortTermMemory` 添加参数有效性校验（`Number.isFinite`），默认值兜底

**变更文件**:
- [apps/api/src/modules/common/redis.service.ts](../apps/api/src/modules/common/redis.service.ts)
- [apps/api/src/modules/agent/memory/memory.service.ts](../apps/api/src/modules/agent/memory/memory.service.ts)

---

### 3. 前轮修复补充

#### WebSocket 认证
`AuthModule` 中 `JwtModule.registerAsync` 添加 `global: true`，`ChatModule` 移除 `JwtModule.register({})` 空配置导入。确保 JwtService 实例唯一且配置正确。

#### Redis 优雅降级
`ChatGatewayService`、`AuthService`、`ChatService` 中 Redis 调用加 try-catch 保护，避免 Redis 不可用时功能中断。

**变更文件**:
- [apps/api/src/modules/auth/auth.module.ts](../apps/api/src/modules/auth/auth.module.ts)
- [apps/api/src/modules/chat/chat.module.ts](../apps/api/src/modules/chat/chat.module.ts)
- [apps/api/src/gateways/chat-gateway.service.ts](../apps/api/src/gateways/chat-gateway.service.ts)
- [apps/api/src/modules/auth/auth.service.ts](../apps/api/src/modules/auth/auth.service.ts)
- [apps/api/src/modules/chat/chat.service.ts](../apps/api/src/modules/chat/chat.service.ts)

---

### 测试结果

| 模块 | 测试套件 | 测试用例 | 状态 |
|------|---------|---------|------|
| 后端 (API) | 19 | 418 | ✅ 全部通过 |
| 前端 (Web) | 10 | 117 | ✅ 全部通过 |

---

### 4. DeepSeek 思考模式参数修复

**根因**: `reasoning_effort` 错误地放在了 `extra_body` 内部，但 DeepSeek 官方 API 要求 `reasoning_effort` 是顶级请求参数，只有 `thinking` 开关放在 `extra_body` 中。

**修复**:
- `buildExtraBody()` 仅保留 `thinking: { type: 'enabled' }`
- `reasoning_effort` 移到 `body` 顶级，与 `model`、`messages` 同级
- 同时在 `chat()` 和 `chatStreamWithReasoning()` 中修正

**变更文件**:
- [apps/api/src/modules/llm/providers/deepseek.provider.ts](../apps/api/src/modules/llm/providers/deepseek.provider.ts)

---

### 测试结果

| 模块 | 测试套件 | 测试用例 | 状态 |
|------|---------|---------|------|
| 后端 (API) | 19 | 418 | ✅ 全部通过 |
| 前端 (Web) | 10 | 117 | ✅ 全部通过 |
